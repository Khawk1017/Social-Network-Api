const User = require('./User');
const Thought = require('./Thought');
// exporting the 'User' and 'Thought' model from correspoing files to be reusable throughout the applocation 
module.exports = { User, Thought}

